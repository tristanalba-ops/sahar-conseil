'use client';import { useState, useEffect } from 'react';import { createClient } from '@supabase/supabase-js';import ProgressBar from '../../components/ProgressBar';import ReportDisplay from '../../components/ReportDisplay';import ComparablesTable from '../../components/ComparablesTable';import PriceCharts from '../../components/PriceCharts';import DPEDistribution from '../../components/DPEDistribution';const supabase = createClient(  process.env.NEXT_PUBLIC_SUPABASE_URL!,  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);interface BanSuggestion {  label: string;  citycode: string;  postcode: string;  name: string;  context: string;}interface ReportData {  code_insee: string;  comparables: any[];  price_by_type: any[];  price_by_dpe: any[];  dpe_distribution: any;  dpe_summary: any;}export default function GeneratePage() {  const [address, setAddress] = useState('');  const [suggestions, setSuggestions] = useState<BanSuggestion[]>([]);  const [selectedAddress, setSelectedAddress] = useState<BanSuggestion | null>(null);
